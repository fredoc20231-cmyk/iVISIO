# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# Scientific / single-cell + spatial stack
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1
anndata==0.10.9
scanpy==1.10.3
leidenalg==0.10.2
igraph==0.11.6
h5py==3.11.0
Pillow==10.4.0

# Optional / advanced-feature analogs. Every feature that depends on these
# checks the import at call time and degrades to a clear message if missing,
# so the platform still starts and runs the core workflow without them.
squidpy==1.6.1          # Moran's I spatial autocorrelation + ligrec (CellChat analog)
gseapy==1.1.3           # GO/pathway enrichment (clusterProfiler analog)
harmonypy==0.0.10       # multi-slice batch integration (Harmony analog)
